# System Architecture Diagram

## Overall System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Client Devices                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  Device A    │  │  Device B    │  │  Device C    │         │
│  │  (Master)    │  │  (Slave)     │  │  (Slave)     │         │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤         │
│  │ Browser      │  │ Browser      │  │ Browser      │         │
│  │ - React UI   │  │ - React UI   │  │ - React UI   │         │
│  │ - Zustand    │  │ - Zustand    │  │ - Zustand    │         │
│  │ - Socket.io  │  │ - Socket.io  │  │ - Socket.io  │         │
│  │ - Audio API  │  │ - Audio API  │  │ - Audio API  │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                 │                 │                  │
│         └─────────────────┼─────────────────┘                  │
│                           │                                    │
└───────────────────────────┼────────────────────────────────────┘
                            │
                            │ WebSocket (Socket.io)
                            │
┌───────────────────────────┼────────────────────────────────────┐
│                           │                                    │
│                    ┌──────▼───────┐                            │
│                    │   Server     │                            │
│                    │  (Node.js)   │                            │
│                    ├──────────────┤                            │
│                    │ - Next.js    │                            │
│                    │ - Socket.io  │                            │
│                    │ - Time Sync  │                            │
│                    │ - Prisma ORM │                            │
│                    └──────┬───────┘                            │
│                           │                                    │
│                    ┌──────▼───────┐                            │
│                    │   Database   │                            │
│                    │  (SQLite)    │                            │
│                    ├──────────────┤                            │
│                    │ - Tracks     │                            │
│                    │ - Playlists  │                            │
│                    │ - Metadata   │                            │
│                    └──────────────┘                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Time Synchronization Flow

```
┌─────────────┐                    ┌─────────────┐
│  Client A   │                    │   Server    │
└──────┬──────┘                    └──────┬──────┘
       │                                  │
       │  1. time-sync-request (t0)      │
       │─────────────────────────────────>│
       │                                  │
       │                                  │ 2. Record server time (t1)
       │                                  │
       │  3. time-sync-response           │
       │     (clientTime, serverTime, t2) │
       │<─────────────────────────────────│
       │                                  │
       │ 4. Calculate offset (t3)         │
       │    RTT = t3 - t0                 │
       │    offset = server - client + RTT/2
       │                                  │
       │ 5. Store offset in Zustand       │
       │                                  │
       ▼                                  ▼

Repeat 5 times, use median offset
```

## Synchronized Playback Flow

```
┌──────────┐         ┌──────────┐         ┌──────────┐         ┌──────────┐
│ Device A │         │ Device B │         │  Server  │         │ Device C │
│ (Master) │         │ (Slave)  │         │          │         │ (Slave)  │
└────┬─────┘         └────┬─────┘         └────┬─────┘         └────┬─────┘
     │                    │                    │                    │
     │ 1. User clicks play                    │                    │
     │                    │                    │                    │
     │ 2. requestSyncPlay(trackId, 0, 150ms)  │                    │
     │────────────────────────────────────────>│                    │
     │                    │                    │                    │
     │                    │                    │ 3. Calculate sync time
     │                    │                    │    syncTime = now + 150ms
     │                    │                    │                    │
     │ 4. sync-play-command                   │                    │
     │<────────────────────────────────────────│                    │
     │                    │                    │                    │
     │                    │ 5. sync-play-command                    │
     │                    │<───────────────────│                    │
     │                    │                    │                    │
     │                    │                    │ 6. sync-play-command
     │                    │                    │───────────────────>│
     │                    │                    │                    │
     │ 7. Calculate delay │                    │                    │
     │    localTime = now + offset            │                    │
     │    delay = syncTime - localTime        │                    │
     │                    │                    │                    │
     │                    │ 8. Calculate delay │                    │
     │                    │    (same as above) │                    │
     │                    │                    │                    │
     │                    │                    │ 9. Calculate delay │
     │                    │                    │    (same as above) │
     │                    │                    │                    │
     │ 10. setTimeout(play, delay)            │                    │
     │                    │                    │                    │
     │                    │ 11. setTimeout(play, delay)            │
     │                    │                    │                    │
     │                    │                    │ 12. setTimeout(play, delay)
     │                    │                    │                    │
     ▼                    ▼                    ▼                    ▼
   PLAY                PLAY                                      PLAY
(at exact same time across all devices)
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         App Layout                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌────────────────────────────────────────┐  │
│  │   Sidebar    │  │         MainContent                    │  │
│  ├──────────────┤  ├────────────────────────────────────────┤  │
│  │ - Home       │  │  ┌──────────────────────────────────┐  │  │
│  │ - Library    │  │  │   Playlist Header                │  │  │
│  │              │  │  │   - Title                        │  │  │
│  │ Server       │  │  │   - [▶] Large Play Button       │  │  │
│  │ - Dashboard  │  │  │   - [...] Menu (Delete)         │  │  │
│  │ - Upload     │  │  └──────────────────────────────────┘  │  │
│  │ - Settings   │  │                                        │  │
│  │              │  │  ┌──────────────────────────────────┐  │  │
│  │ Playlists    │  │  │   Track List                     │  │  │
│  │ - Liked [▶]  │  │  │   ┌────┬──────────┬──────────┐  │  │  │
│  │ - My List[▶] │  │  │   │ #  │ TITLE    │ ALBUM    │  │  │  │
│  │ - Chill  [▶] │  │  │   ├────┼──────────┼──────────┤  │  │  │
│  │              │  │  │   │ 1  │ Song A   │ Album X  │  │  │  │
│  └──────────────┘  │  │   │[▶] │          │          │  │  │  │
│                    │  │   │ 2  │ Song B   │ Album Y  │  │  │  │
│                    │  │   └────┴──────────┴──────────┘  │  │  │
│                    │  └──────────────────────────────────┘  │  │
│                    └────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    PlayerBar                            │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  [◀] [▶] [▶▶]  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  🔊 ▓▓▓  │   │
│  │  Song Title - Artist                    3:45 / 4:12     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

Hidden Components:
- SyncPlaybackController (no UI, manages sync)
- AudioPlayer (hidden audio element)
- useSocket hook (WebSocket management)
```

## State Management (Zustand)

```
┌─────────────────────────────────────────────────────────────────┐
│                      MusicStore (Zustand)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Playback State:                                                │
│  ├─ currentTrack: Track | null                                  │
│  ├─ isPlaying: boolean                                          │
│  ├─ progress: number (0-100)                                    │
│  ├─ currentTime: number (seconds)                               │
│  ├─ volume: number (0-100)                                      │
│  └─ isMuted: boolean                                            │
│                                                                 │
│  Playlist State:                                                │
│  ├─ currentPlaylist: Track[]                                    │
│  ├─ playlists: Playlist[]                                       │
│  └─ likedSongs: string[]                                        │
│                                                                 │
│  Sync State:                                                    │
│  ├─ isSyncMode: boolean                                         │
│  ├─ connectedDevices: number                                    │
│  └─ serverTimeOffset: number (milliseconds)                     │
│                                                                 │
│  Actions:                                                       │
│  ├─ setCurrentTrack()                                           │
│  ├─ setIsPlaying()                                              │
│  ├─ playPause()                                                 │
│  ├─ playNext()                                                  │
│  ├─ playPrevious()                                              │
│  ├─ playFromPlaylist(tracks, index)                             │
│  ├─ setIsSyncMode()                                             │
│  └─ setServerTimeOffset()                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Socket.io Events

```
┌─────────────────────────────────────────────────────────────────┐
│                      Socket.io Events                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Time Synchronization:                                          │
│  ├─ time-sync-request (client → server)                         │
│  └─ time-sync-response (server → client)                        │
│                                                                 │
│  Basic Playback Control:                                        │
│  ├─ play (broadcast)                                            │
│  ├─ pause (broadcast)                                           │
│  ├─ seek (broadcast)                                            │
│  ├─ track-change (broadcast)                                    │
│  └─ volume-change (broadcast)                                   │
│                                                                 │
│  Synchronized Playback:                                         │
│  ├─ sync-play-request (client → server)                         │
│  ├─ sync-play-command (server → all clients)                    │
│  ├─ sync-next-track (client → server)                           │
│  └─ sync-track-change (server → all clients)                    │
│                                                                 │
│  Library Updates:                                               │
│  └─ library-update (server → all clients)                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow: Play Button Click

```
User clicks large play button in playlist header
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 1. MainContent.tsx - handlePlaylistPlay()                       │
│    - Get playlist tracks from API                               │
│    - Call setCurrentPlaylist(tracks)                            │
│    - Call onTrackSelect(tracks[0])                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Zustand Store - Update State                                 │
│    - currentTrack = tracks[0]                                   │
│    - currentPlaylist = tracks                                   │
│    - isPlaying = true                                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. useSocket Hook - Detect State Change                         │
│    - prevIsPlayingRef !== isPlaying                             │
│    - Emit 'play' event to server                                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Server - Broadcast to All Clients                            │
│    - Receive 'play' event                                       │
│    - socket.broadcast.emit('play', data)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Other Clients - Receive Play Event                           │
│    - Update isPlaying = true                                    │
│    - Audio element starts playing                               │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. SyncPlaybackController - High-Precision Sync                 │
│    - requestAnimationFrame loop                                 │
│    - Check drift every 100ms                                    │
│    - Correct if drift > 50ms                                    │
└─────────────────────────────────────────────────────────────────┘
```

## Performance Optimization

```
┌─────────────────────────────────────────────────────────────────┐
│                   Performance Optimizations                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. Debouncing:                                                 │
│     - Volume changes: 300ms debounce                            │
│     - Search input: 300ms debounce                              │
│                                                                 │
│  2. Throttling:                                                 │
│     - Sync checks: Every 100ms                                  │
│     - Progress updates: 60fps (requestAnimationFrame)           │
│                                                                 │
│  3. Memoization:                                                │
│     - useCallback for event handlers                            │
│     - useMemo for computed values                               │
│                                                                 │
│  4. Virtual Scrolling:                                          │
│     - Large track lists use virtual scrolling                   │
│     - Only render visible items                                 │
│                                                                 │
│  5. Lazy Loading:                                               │
│     - Artwork images load on demand                             │
│     - Audio files stream, not preloaded                         │
│                                                                 │
│  6. Singleton Pattern:                                          │
│     - Single Socket.io connection shared across components      │
│     - Prevents multiple connections                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

This architecture ensures:
- ✅ Millisecond-precision synchronization
- ✅ Scalable to many devices
- ✅ Efficient network usage
- ✅ Smooth UI performance
- ✅ Reliable playback control
